#include<stdio.h>
int main()
{
    int n;
    printf("Enter a number:\n");
    scanf("%d", &n);
    printf("%d", sumofseries(n));
    return 0;
}
int sumofseries(int x)
{
    int i, sum = 0;
    for(i=1;i<=x;i++)
    {
        sum += i*i;
    }
    return sum;
}
